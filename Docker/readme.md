# Instructions for docker
1. Install docker if you dont have it allready: https://docs.docker.com/engine/install/
2. Open an terminal in this directory
3. Run: docker compose up -d
4. Post your request towards: http://localhost:8080
5. To stop everything: docker compose down